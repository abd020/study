# ---------------------------------------------------------------------
# Frontend (Vite) — ces valeurs sont publiques par conception.
# Toute la sécurité repose sur les politiques RLS de PostgreSQL.
# ---------------------------------------------------------------------
VITE_SUPABASE_URL=https://votre-projet.supabase.co
VITE_SUPABASE_ANON_KEY=votre-cle-anon

# ---------------------------------------------------------------------
# Serveur uniquement — NE JAMAIS préfixer par VITE_.
# À déclarer dans les secrets Supabase, jamais dans ce fichier en prod :
#   supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
# La clé n'est lue que par l'Edge Function generate-study-material.
# ---------------------------------------------------------------------
# ANTHROPIC_API_KEY=sk-ant-...
